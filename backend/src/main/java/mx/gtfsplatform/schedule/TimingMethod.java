package mx.gtfsplatform.schedule;

/** Los 3 métodos de la sección 16 del prompt para calcular tiempos entre paradas. */
public enum TimingMethod {
    MANUAL_SEGMENTS,
    AVERAGE_SPEED,
    TOTAL_TRIP_TIME
}
